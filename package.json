{
  "name": "figma-selection-style-exporter",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin that downloads selected nodes, descendants, and style data as JSON.",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "npm run build && node --test test/*.test.js",
    "check": "npm run build && node --check code.js"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "esbuild": "^0.28.0",
    "typescript": "^5.4.5"
  }
}
